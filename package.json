{
  "name": "cloudflare-redirects",
  "version": "1.0.0",
  "description": "Manage Cloudflare subdomain redirects via configuration file",
  "main": "scripts/sync-redirects.js",
  "scripts": {
    "sync": "node scripts/sync-redirects.js",
    "validate": "node scripts/validate-redirects.js",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  },
  "author": "",
  "license": "MIT"
}
